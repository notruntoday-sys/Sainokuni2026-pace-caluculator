# 彩の国100mile ペース計算ツール仕様書

最終更新: 2026-05-18  
対象実装: `index.html` / `styles.css` / `script.js`  
公開URL: https://sainokuni-100mile.vercel.app

## 1. ツール概要

彩の国100mileのための静的Webペース計算ツール。ソロ100mileと駅伝に対応し、目標時間ごとの到着時刻、出発時刻、区間所要時間、区間ペース、関門、ライト推奨時間、累積上昇・下降標高、駅伝メンバー別走行時間、待ち受け画像を表示する。

アプリ名は「彩の国100mile ペース計算ツール」。画面上のバージョン表記は `v1.2 Sainokuni pace table`。

## 2. 技術構成

- 静的HTML/CSS/JavaScriptのみで構成する。
- ビルド工程は不要。
- Vercelでは静的ファイルとして公開する。
- 主要ファイル:
  - `index.html`: 画面構造、フォーム、表、フッター、OFUSEリンク。
  - `styles.css`: レイアウト、色分け、レスポンシブ表示、表、累積標高図、待ち受け表示。
  - `script.js`: コースデータ、計算ロジック、描画、設定保存、待ち受けSVG/PNG生成。
  - `vercel.json`: 静的サイト用の設定。
  - `README.md`: 簡易説明。

## 3. コース概要

画面上部に以下の概要チップを表示する。

- 距離: `162.8 km`
- 累積: `約10,000 m`
- スタート: `ソロ 7:00 / 駅伝 8:00`
- 構成: `North + South1 + South2`

内部のコース合計値は以下。

| 項目 | 値 |
|---|---:|
| 総距離 | 162.8km |
| 表示用累積上昇 | 10,000m |
| GPX累積上昇 | 10,060m |
| GPX累積下降 | 10,059m |

## 4. 部門仕様

| 部門 | ラベル | スタート時刻 | 最大表示時間 | 関門 |
|---|---|---:|---:|---|
| ソロ | ソロ | 07:00 | 35時間 | `soloCutoff` を使用 |
| 駅伝 | 駅伝 | 08:00 | 34時間 | `relayCutoff` を使用 |

駅伝はソロより1時間遅いスタートとして扱う。駅伝選択時は35時間の選択肢を無効化し、34時間まで表示する。

## 5. 入力フォーム仕様

### 5.1 部門

`#race-category`

- `solo`: ソロ。初期選択。
- `relay`: 駅伝。

### 5.2 休憩パターン

`#rest-pattern`

- `短め`
- `標準`: 初期選択。
- `長め`

### 5.3 ペース戦略

`#strategy`

- `イーブン`: 初期選択。
- `後半10%落ち`
- `後半20%落ち`
- `後半30%落ち`
- `後半40%落ち`
- `9・11・13時間`

### 5.4 最短時間

`#min-hour`

- 選択肢は24時間から35時間まで。
- 初期値は24時間。
- 部門が駅伝の場合、35時間は無効化される。

### 5.5 最長時間

`#max-hour`

- 選択肢は24時間から35時間まで。
- 初期値は35時間。
- 部門が駅伝の場合、35時間は無効化され、選択済みなら34時間に補正される。

### 5.6 ボタン

- `再計算`: 現在の入力で表、駅伝メンバー表、通知文を再描画する。
- `待ち受け表示`: 選択中の最短時間から最大3列分の待ち受け画像を生成する。

## 6. コースデータ

各地点は `baseSections` に定義する。`dist` は直前地点からの区間距離、`coef` はペース配分の難度係数、`base` はニューサンピア帰着地点を表す。

| Index | エイド | Loop | Group | 区間km | 係数 | Base | ソロ関門(分) | 駅伝関門(分) | 100km参考(分) |
|---:|---|---|---|---:|---:|---|---:|---:|---:|
| 0 | Start:ニューサンピア | Start | start | 0.0 | 1.00 |  |  |  |  |
| 1 | くぬぎむら体験交流館 | North | north | 14.3 | 1.00 |  |  |  | 350 |
| 2 | 慈光寺 | North | north | 6.1 | 1.15 |  |  |  | 460 |
| 3 | 堂平キャンプ場 | North | north | 11.8 | 1.22 |  |  |  | 690 |
| 4 | 刈場坂峠 | North | north | 7.1 | 0.98 |  |  |  | 790 |
| 5 | ニューサンピア | North終わり | north | 14.5 | 1.00 | yes |  | 660 | 990 |
| 6 | 桂木観音 | South1 | south1 | 8.2 | 0.95 |  |  |  | 1090 |
| 7 | 高山不動尊上の旧茶屋 | South1 | south1 | 8.5 | 1.18 |  |  |  | 1250 |
| 8 | 竹寺 | South1 | south1 | 10.5 | 1.24 |  |  |  | 1440 |
| 9 | kinoca(東吾野) | South1 | south1 | 11.0 | 1.06 |  |  |  | 1660 |
| 10 | 桂木観音 | South1 | south1 | 11.2 | 1.12 |  |  |  | 1880 |
| 11 | ニューサンピア | South1終わり | south1 | 5.0 | 0.92 | yes | 1350 | 1290 | 1980 |
| 12 | 桂木観音 | South2 | south2 | 8.2 | 1.02 |  | 1440 | 1380 |  |
| 13 | 高山不動尊上の旧茶屋 | South2 | south2 | 8.5 | 1.18 |  | 1560 | 1500 |  |
| 14 | 竹寺 | South2 | south2 | 10.5 | 1.24 |  | 1690 | 1630 |  |
| 15 | kinoca(東吾野) | South2 | south2 | 11.2 | 1.10 |  | 1860 | 1800 |  |
| 16 | 桂木観音 | South2 | south2 | 11.2 | 1.18 |  | 2025 | 1965 |  |
| 17 | Goal:ニューサンピア | Finish | finish | 5.0 | 0.98 |  | 2100 | 2040 |  |

関門分は各部門のスタートからの経過分として扱う。表示上は `ソロ関門 22:30 (翌05:30)` のように経過時間と実時刻を併記する。100km参考は7:00スタート基準の時計表示として表示する。

## 7. 休憩時間仕様

通常エイドとニューサンピア帰着地点で滞在時間を分ける。

| 休憩パターン | 通常エイド | North終わりニューサンピア | South1終わりニューサンピア |
|---|---:|---:|---:|
| 短め | 3分 | 15分 | 20分 |
| 標準 | 5分 | 20分 | 25分 |
| 長め | 8分 | 30分 | 35分 |

始点とゴールは滞在0分。

駅伝の場合、通常戦略ではニューサンピア帰着地点の滞在を以下に補正する。

| 駅伝休憩パターン | ニューサンピア滞在 |
|---|---:|
| 短め | 1分 |
| 標準 | 2分 |
| 長め | 2分 |

ただし、ペース戦略が `9・11・13時間` の場合は、部門にかかわらず `base: true` のニューサンピア帰着地点を30分滞在に上書きする。

## 8. ペース計算仕様

### 8.1 基本式

各区間の基本重みを以下で算出する。

```text
baseWeight = section.dist * section.coef
```

各表示時間 `h` に対して、予定走行時間を以下で算出する。

```text
totalPlannedRunMins = h * 60 - totalStayTime
paceUnit = totalPlannedRunMins / totalWeight
runTime(section) = sectionWeight * paceUnit
```

各地点の到着・出発は以下で更新する。

```text
arrival[i] = previousDeparture + runTime[i]
departure[i] = arrival[i] + stayTime[i]
```

### 8.2 イーブン

全区間で `baseWeight` をそのまま使う。

### 8.3 後半落ち戦略

全コースの重み合計の半分を境に、区間重みの中点が後半側に入る区間へ倍率をかける。

| 戦略 | 後半区間の倍率 |
|---|---:|
| 後半10%落ち | 1.1 |
| 後半20%落ち | 1.2 |
| 後半30%落ち | 1.3 |
| 後半40%落ち | 1.4 |

区間の中点判定:

```text
mid = cumulativeWeightBeforeSection + baseWeight / 2
if mid > totalBaseWeight / 2 then multiplier applies
```

### 8.4 9・11・13時間戦略

周回ごとの目標時間を固定した配分戦略。

| Loop | 目標時間 |
|---|---:|
| North | 9時間 |
| South1 | 11時間 |
| South2 | 13時間 |

対象区間:

| Loop | 区間Index |
|---|---|
| North | 1-5 |
| South1 | 6-11 |
| South2 | 12-17 |

周回内の通常エイド滞在は目標時間に含める。周回終点のニューサンピア滞在は次周回前の休憩として扱い、周回時間には含めない。

計算式:

```text
loopRawWeight = sum(baseWeight in loop)
loopInternalStay = sum(stayTime in loop except loop end)
targetRunTime = loopTargetHours * 60 - loopInternalStay
sectionWeight = targetRunTime * baseWeight / loopRawWeight
```

34時間列では以下になる。

| 地点 | 経過 | 実時刻(ソロ) | 備考 |
|---|---:|---:|---|
| North終わりニューサンピア着 | 9:00 | 16:00 | North 9時間 |
| North終わりニューサンピア発 | 9:30 | 16:30 | 30分休憩 |
| South1終わりニューサンピア着 | 20:30 | 翌03:30 | South1 11時間 |
| South1終わりニューサンピア発 | 21:00 | 翌04:00 | 30分休憩 |
| Goal | 34:00 | 翌17:00 | South2 13時間 |

表示時間が34時間以外の場合も、9:11:13の周回比率を保ったまま全体時間に合わせて伸縮する。

## 9. 関門補正仕様

関門が設定された地点で、出発時刻が関門を超える場合は以下の補正を行う。

```text
departure = cutoffAbs - 1
arrival = departure - stayTime
```

つまり、関門1分前に出発するよう補正する。

到着自体が関門後だった場合は `isArrivalCapped` を立て、到着表示を赤系で強調する。出発が補正された場合は `isDepartureCapped` を立て、出発表示を黄色斜線で強調する。

補正後、残り区間のペースを再計算する。

```text
remainingWeight = totalWeight - weightsFinished
timeUsedForRunSoFar = departure - startMinutesAbsolute - staysFinished
remainingRunTime = totalPlannedRunMins - timeUsedForRunSoFar
currentPaceUnit = remainingRunTime / remainingWeight
```

## 10. ライト推奨時間仕様

ライト推奨時間帯は以下。

```text
18:45-04:35
```

到着または出発がこの時間帯に入るエイドセル、または区間の出発から到着までの間にこの時間帯を含む区間セルをライト推奨表示にする。

判定は絶対分を24時間で割った日内分で行う。

```text
dayMinute >= 18:45 または dayMinute <= 04:35
```

区間判定は開始分から終了分まで60分刻みで走査し、最後に終了時刻も確認する。

## 11. 表示仕様

### 11.1 メイン表

ヘッダー:

```text
エイド / 区間 / 累積 / 上昇下降 / 滞在 / {h}h ペース...
```

エイド行:

- エイド名
- Loop名
- 関門があれば部門別関門
- 100km参考時刻があれば参考表示
- 区間距離 / 累積距離
- 累積上昇 / 累積下降を `+ / -` 形式で表示
- 滞在分
- 各表示時間の到着時刻と出発時刻

始点は `+0 / -0` を表示する。

区間行:

- ラベルは `区間`
- 区間距離
- 区間上昇 / 区間下降を `+ / -` 形式で表示
- 各表示時間の区間所要時間とペース

### 11.2 時刻フォーマット

経過時間と実時刻を併記する。

```text
HH:MM (HH:MM)
```

エイド行では到着と出発を以下の形式で表示する。

```text
06:57 (13:57)着
07:27 (14:27)発
```

ゴール行は到着のみ表示する。

### 11.3 区間ペースフォーマット

```text
所要時間 (m:ss/km)
```

例:

```text
1:38 (6:44/km)
```

### 11.4 凡例

画面下部に以下を表示する。

- North
- South1
- South2
- 関門1分前に出発するよう補正
- 関門前に到着するよう補正
- ライト推奨時間帯 18:45-04:35

## 12. 駅伝メンバー別走行時間

部門が駅伝でない場合は非表示。部門が駅伝の場合のみ、メイン表の上に表示する。

### 12.1 担当区間

| 担当 | From | To | 距離 | 上昇 | 下降 |
|---|---|---|---:|---:|---:|
| North | Start:ニューサンピア | ニューサンピア(North終わり) | 53.8km | +3317m | -3321m |
| South1 | ニューサンピア(North終わり) | ニューサンピア(South1終わり) | 54.4km | +3361m | -3359m |
| South2 | ニューサンピア(South1終わり) | Goal:ニューサンピア | 54.6km | +3382m | -3379m |

### 12.2 表示内容

各表示時間ごとに以下を表示する。

- 走行時間
- 経過時間: `経 start→end`
- 実時刻: `時 startClock→endClock`

交代地点の滞在時間は含めない。前担当の出発時刻から後担当の到着時刻までではなく、各担当の出発から到着までを走行時間として扱う。

## 13. 累積標高図

GPXを1kmごとに集計した標高データを `elevationProfile` として保持する。細かな標高ノイズを抑えるため、8mフィルタで累積上昇を算出している。

表示仕様:

- SVGで描画する。
- 描画サイズは内部的に `1000 x 260`。
- 緑線: 累積上昇。
- 青線・淡青塗り: 標高プロファイル。
- Y軸目盛: `0 / 2,500 / 5,000 / 7,500 / 10,000`
- X軸: `0km` と `162.8km`
- 周回区切りとして `North`、`South1`、`Finish` の縦線ラベルを表示する。
- 右上に `緑: 累積上昇 / 青: 標高` と表示する。
- 累積値表示は `GPX累積 +10,060m`。

## 14. 区間標高データ

`elevationSectionStats` は各地点Indexに対応する。`gain/loss` は直前地点からの区間上昇下降、`cumGain/cumLoss` は当該地点までの累積上昇下降。

| Index | 地点 | 標高m | 区間上昇 | 区間下降 | 累積上昇 | 累積下降 |
|---:|---|---:|---:|---:|---:|---:|
| 0 | Start:ニューサンピア | 108 | 0 | 0 | 0 | 0 |
| 1 | くぬぎむら体験交流館 | 442 | 1024 | 686 | 1024 | 686 |
| 2 | 慈光寺 | 345 | 317 | 419 | 1341 | 1105 |
| 3 | 堂平キャンプ場 | 868 | 1234 | 704 | 2575 | 1809 |
| 4 | 刈場坂峠 | 827 | 432 | 474 | 3007 | 2283 |
| 5 | ニューサンピア(North終わり) | 107 | 310 | 1038 | 3317 | 3321 |
| 6 | 桂木観音 | 236 | 438 | 299 | 3755 | 3620 |
| 7 | 高山不動尊上の旧茶屋 | 756 | 779 | 263 | 4534 | 3883 |
| 8 | 竹寺 | 577 | 622 | 799 | 5156 | 4682 |
| 9 | kinoca(東吾野) | 144 | 671 | 1110 | 5827 | 5792 |
| 10 | 桂木観音 | 278 | 690 | 553 | 6517 | 6345 |
| 11 | ニューサンピア(South1終わり) | 108 | 161 | 335 | 6678 | 6680 |
| 12 | 桂木観音 | 246 | 444 | 307 | 7122 | 6987 |
| 13 | 高山不動尊上の旧茶屋 | 772 | 779 | 251 | 7901 | 7238 |
| 14 | 竹寺 | 568 | 604 | 811 | 8505 | 8049 |
| 15 | kinoca(東吾野) | 130 | 699 | 1134 | 9204 | 9183 |
| 16 | 桂木観音 | 291 | 704 | 541 | 9908 | 9724 |
| 17 | Goal:ニューサンピア | 106 | 152 | 335 | 10060 | 10059 |

表ではエイド標高そのものは表示しない。エイド行では累積上昇下降のみを `+ / -` で表示する。

## 15. 設定保存

ブラウザの `localStorage` に以下のキーで保存する。

```text
sainokuni-100mile-pace-settings-v2
```

保存対象:

- raceCategory
- restPattern
- strategy
- minHour
- maxHour

読み込みに失敗した場合は警告をコンソールへ出し、初期値で継続する。保存値が現在の選択肢に存在しない場合は、その値は復元しない。

## 16. 待ち受け画像仕様

`待ち受け表示` ボタンでSVGを生成し、画面上にプレビューする。PNG保存ボタンでPNGへ変換する。

### 16.1 対象列

選択中の最短時間から最大3列を出力する。

```text
wallpaperMax = min(minHour + 2, category.maxHour)
```

例:

- ソロ、最短24時間なら `24h / 25h / 26h`
- 駅伝、最短33時間なら `33h / 34h`

### 16.2 画像仕様

- SVG内部サイズ: `1170 x 2532`
- PNG出力サイズも `1170 x 2532`
- ファイル名: `sainokuni-100mile-{categoryKey}-{minHour}-{wallpaperMax}h.png`
- タイトル: `彩の国100mile`
- サブタイトル: `{部門} / Start {時刻} / Rest {休憩パターン} / {ペース戦略}`
- バッジ: `162.8km`、`+10,000m`、`18:45-04:35`
- エイド行のみを出力する。
- 列は最大3列。
- 関門補正、ライト推奨は色や背景で反映する。

### 16.3 PNG保存

`navigator.canShare` と `navigator.share` が利用可能な環境では共有シートを優先する。利用できない場合は通常のダウンロードリンクでPNGを保存する。

## 17. デザイン仕様

### 17.1 色分け

主要なコース区分は背景色と左線で判別する。

- North: 緑系
- South1: 黄系
- South2: 青系
- 関門出発補正: 黄色斜線
- 関門到着補正: 赤系
- ライト推奨: グレー斜線

### 17.2 レイアウト

- 全体背景は淡いグリーン系。
- テーブルは横スクロール可能。
- 操作フォームはカード状の白背景。
- 表ヘッダーは濃紺系背景、白文字。
- カード角丸は控えめ。
- スマホ幅では文字サイズ、表幅、メンバー表の列幅を調整する。

### 17.3 スマホ対応

- メイン表は横スクロール。
- 駅伝メンバー表も横スクロール。
- 駅伝メンバー表の時間列は `min-width: 112px` を確保し、24hセルなどで文字が枠外にはみ出しにくいようにする。
- 累積標高図内の文字はモバイルで小さくする。
- 下部の表テキストは過密にならないようフォントサイズを段階的に下げる。

## 18. フッター・リンク

フッターには以下を表示する。

- `Presented by 今日は走らないでビール`
- Spotifyリンク: https://open.spotify.com/show/0j1TXvIRRcwkbfsRHjM5qT
- Apple Podcastsリンク: https://podcasts.apple.com/jp/podcast/今日は走らないでビール/id1655012487
- Podcast説明文:
  - `トレイルランやレースの話を、ビール片手にゆるく話しているポッドキャストです。レースの振り返りやトレイルランの話は、ポッドキャストでもゆるく話しています。`
- データ注記:
  - `距離・関門は第11回トレニックワールド100mile & 100km in 彩の国の2026大会概要をもとにしています。`
- 参照リンク:
  - https://trainic-world.org/sainokuni/%E5%A4%A7%E4%BC%9A%E6%A6%82%E8%A6%812026
- OFUSEリンク:
  - https://ofuse.me/o?uid=163955
  - ボタン文言: `OFUSEで応援を送る`

## 19. 初期表示

初期状態は以下。

| 項目 | 初期値 |
|---|---|
| 部門 | ソロ |
| 休憩パターン | 標準 |
| ペース戦略 | イーブン |
| 最短時間 | 24時間 |
| 最長時間 | 35時間 |

初回表示時に以下を実行する。

1. 時間セレクトを24-35時間で生成する。
2. 保存済み設定があれば復元する。
3. 部門に応じて最大時間を同期する。
4. 表、駅伝メンバー表、累積標高図、通知文を描画する。

## 20. 通知文仕様

フォーム下の通知文には以下を表示する。

```text
{部門} / 表示範囲 {minHour}h-{maxHour}h / 休憩合計 {totalStayTime}分 / スタート {start} / ライト 18:45-04:35 / コース 162.8km +10,000m
```

例:

```text
ソロ / 表示範囲 24h-35h / 休憩合計 177分 / スタート 07:00 / ライト 18:45-04:35 / コース 162.8km +10,000m
```

## 21. データソース

- 距離、関門: 第11回トレニックワールド100mile & 100km in 彩の国 2026大会概要。
- 累積標高、標高図: `26Sainokuni-100mile.gpx.xml` をもとに、1kmごとに集計し、8mフィルタで累積上昇を算出したデータ。

## 22. 既知の仕様上の注意

- `COURSE_TOTALS.gain` は画面概要・通知・待ち受けでは丸めた `10,000m` を表示する。
- GPX由来の累積標高図では `+10,060m` を表示する。
- メイン表ではエイド標高そのものは表示しない。累積上昇下降のみを表示する。
- 100km参考時刻は参考表示であり、計算や関門補正には使わない。
- 関門補正は「出発」を関門1分前にするため、滞在時間がある地点では到着時刻も前倒しされる。
- 駅伝メンバー別走行時間は、交代地点での滞在時間を含まない。
- `9・11・13時間` 戦略ではニューサンピア休憩が30分に固定されるため、通常の休憩パターンや駅伝用ニューサンピア短縮休憩より優先される。

## 23. 公開・リポジトリ

- GitHubリポジトリ: https://github.com/notruntoday-sys/Sainokuni2026-pace-caluculator
- Vercel本番URL: https://sainokuni-100mile.vercel.app
- Vercelプロジェクト名: `sainokuni-100mile`

